---
- name: Harbor OCI Chart Synchronization
  hosts: "{{ 'harbor_origin_oci' if 'harbor_origin_oci' in groups else [] }}"
  gather_facts: false
  become: true
  roles:
    - role: provision_harbor_origin
