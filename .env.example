# Environment
HOST_UID=1000  # Specifies the host user identifier obtainable via id -u execution.
HOST_GID=1000  # Specifies the host group identifier obtainable via id -g execution.
SONAR_DB_PASSWORD=changeme  # Shared secret between the sonarqube and sonarqube-db services; generate via openssl rand -base64 24.
